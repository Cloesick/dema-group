{
  "framework": "nextjs",
  "buildCommand": "node scripts/check-deployment.js && NEXT_TELEMETRY_DISABLED=1 NODE_OPTIONS='--max_old_space_size=3072' next build || node scripts/handle-build-error.js",
  "installCommand": "pnpm install --no-frozen-lockfile --prefer-offline",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "build": {
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1",
      "NEXT_SHARP_PATH": "/tmp/node_modules/sharp",
      "NODE_ENV": "production"
    }
  }
}
